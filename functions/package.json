{
  "name": "jeutaime-functions",
  "description": "Cloud Functions backend for JeuTaime app",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint . --ext .ts",
    "test": "jest",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^4.0.0",
    "stripe": "^12.0.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.4",
    "typescript": "^5.0.0"
  }
}
